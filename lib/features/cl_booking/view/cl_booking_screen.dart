import 'package:flutter/material.dart';

class ClBookingScreen extends StatelessWidget {
  const ClBookingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Booking'),
      ),
    );
  }
}
