import 'package:flutter/material.dart';

class SmOffersScreen extends StatelessWidget {
  const SmOffersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('SmOffers Screen'),
    );
  }
}
