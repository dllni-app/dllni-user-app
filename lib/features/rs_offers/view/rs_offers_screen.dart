import 'package:flutter/material.dart';

class RsOffersScreen extends StatelessWidget {
  const RsOffersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('RsOffers Screen'),
    );
  }
}
