import 'package:flutter/material.dart';

class RsOrdersScreen extends StatelessWidget {
  const RsOrdersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('RsOrders Screen'),
    );
  }
}
