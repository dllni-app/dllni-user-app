import 'package:flutter/material.dart';

class RsHomeScreen extends StatelessWidget {
  const RsHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('RsHome Screen'),
    );
  }
}
