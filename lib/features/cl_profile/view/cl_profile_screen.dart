import 'package:flutter/material.dart';

class ClProfileScreen extends StatelessWidget {
  const ClProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Profile'),
      ),
    );
  }
}
