import 'package:flutter/material.dart';

class ClOffersScreen extends StatelessWidget {
  const ClOffersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Offers'),
      ),
    );
  }
}
