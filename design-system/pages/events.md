# Events override

- Render event types and fields in API `sortOrder`; retain answers across validation/network failures.
- Multi-day sessions are independently readable and selectable. Each card exposes date, time, required/accepted workers, travel, and session state.
- Worker review occurs after the event from one screen, with one entry for every unique worker and a visible incomplete count.

